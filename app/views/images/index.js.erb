$("#pagination-content").html('<%= j(render "images_table", images: @images) %>')
if ($(window).scrollTop() > 300) {
  $('html, body').animate({
      scrollTop: $("#pagination-content").offset().top
  }, 300);
}