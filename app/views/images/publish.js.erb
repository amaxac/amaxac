var row = $('.image-row[data-id="<%= @image.id %>"]');
row.filter(".btn-approve").remove();
row.clearQueue().queue(function (next) {
    $(this).addClass("highlight"); next();
}).delay(500).queue(function (next) {
    $(this).removeClass("highlight"); next();
});